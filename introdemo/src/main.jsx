import ReactDOM from 'react-dom/client'

import App from './App'

// render the App component into the root element (a div with id root defined in index.html)
ReactDOM.createRoot(document.getElementById('root')).render(<App />)